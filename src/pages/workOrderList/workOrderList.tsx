import { Text } from "react-native-paper";
import { SafeAreaView } from "react-native-safe-area-context";


function WorkOrderList() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <Text>WorkOrderList</Text>
        </SafeAreaView>
    )
}

export default WorkOrderList; 